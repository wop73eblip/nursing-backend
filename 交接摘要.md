# 護理排班系統 — 完整交接摘要

> 更新:2026-08-05。可直接複製到新對話框繼續。全部檔案在本機 `C:\Users\user\nursing-system\`（無 /mnt/user-data/outputs）。

---

## 一、專案架構

**類型**:醫院護理排班系統。四角色:`nurse` / `dual`(管理員兼護理師) / `admin` / `superadmin`。已正式上線、免費託管。

| 層 | 技術 | 路徑 |
|---|---|---|
| 前端 | React 19 + TypeScript + Vite 8 + React Router 7；axios、dayjs、xlsx | `frontend\` |
| 後端 | Python FastAPI + Uvicorn；**Google OR-Tools CP-SAT**；openpyxl、bcrypt/passlib、python-jose、python-dotenv | `backend\main.py`（單檔 ~2000+ 行） |
| 資料庫 | Supabase(PostgreSQL) | https://swziremayhtrffxltcjz.supabase.co |

**部署現況**
- 正式網站(給大家用):**https://nursing-system.pages.dev**（Cloudflare Pages）
- 後端 API:https://web-production-e9f2a5.up.railway.app（Railway，region sfo）
- 帳號都用 habitwop73@gmail.com（Google/GitHub 登入）

**部署指令**
```bash
# 前端（一定要在 frontend 目錄，否則 wrangler 找不到 dist）
cd frontend && npm run build && npx wrangler pages deploy dist --project-name=nursing-system --branch=main --commit-dirty=true
# 後端
cd backend && npx @railway/cli up --detach   # 再 npx @railway/cli deployment list 確認 SUCCESS
```

---

## 二、關鍵檔案清單

```
C:\Users\user\nursing-system\
├─ backend\
│   ├─ main.py              ← 全部後端(API + CP-SAT + Excel)，單檔
│   ├─ .env                 ← SUPABASE_URL/ANON_KEY/SERVICE_KEY/SECRET_KEY/DATABASE_PASSWORD
│   │                          ⚠️ DATABASE_PASSWORD 已失效(直連 Postgres 會 auth failed)
│   └─ venv\Scripts\python.exe   ← 本機測試(已裝 psycopg2-binary)
├─ frontend\src\
│   ├─ App.tsx              ← 路由 /login /home /nurse /admin
│   ├─ auth.ts / api.ts
│   └─ pages\
│       ├─ LoginPage.tsx    ← 只剩帳密+登入
│       ├─ HomePage.tsx     ← 登入後首頁:模組卡片(排班系統/學習系統)
│       ├─ NursePage.tsx    ← 護理師預班
│       └─ AdminPage.tsx    ← 後台(手動填寫/週期/規則/一鍵生成/帳號/班別/首頁模組/操作紀錄)
├─ 交接摘要.md              ← 本檔
└─ 提示訊息清單.md
記憶檔:C:\Users\user\.claude\projects\C--Users-user-nursing-system\memory\
規則說明頁(Artifact):https://claude.ai/code/artifact/52d54c9e-2b3d-487e-b33a-fb942b4a2b60
```

---

## 三、已完成功能(本次對話)

1. **行政人員**(`users.admin_staff`):可預班但不參與生成。書/會/公 行政班「視同 D 但不計臨床人數」(bug 已修:曾佔 S1 名額導致病房少一人)。
2. **登入後首頁 `/home`**:iOS 風卡片模組選擇(Squircle、2 欄 grid)。後台「首頁模組」分頁可改大標/小標/圖片(存 `rules.data.modules`，GET `/home-config`)。登入頁精簡成只有帳密。
3. **一例一休(H3)= 正常週剛好 2 天休**:下限 `week_rest_sum>=2`(硬，受 `one_in_seven` 勾選) + 上限凸性軟懲罰(全職)。半職不套。超過 2 只來自特殊假/國定假日。
4. **固定班偏離開關**(`allow_fixed_deviation`):勾=最多 2 格、未勾=0。fair 一律 0。
5. **反向班禁止**:固定硬規則(移除勾選，`no_reverse=True`)。
6. **失敗診斷升級**:infeasible-core(assumption 反查衝突規則+護理師+日期) + 診斷解救援(逾時→移除目標函數重解) + 預班衝突提示。
7. **新人+導師**(`users.is_trainee` / `mentor_uid`):照所有規則排、不計臨床人數、軟性跟隨 leader 導師。
8. **UI**:帳號卡片 5 行版面；後台班表尺寸統一成護理師介面(姓名70/字15、班屬34、日期42、格36×30)；日期表頭三行(月/日/週單字)且置中。
9. **規則說明頁**:重新編號 硬規則 H1–H13、軟規則 S1–S8。

**已跑過的 DB migration(Supabase SQL Editor)**
```sql
ALTER TABLE users ADD COLUMN IF NOT EXISTS admin_staff boolean NOT NULL DEFAULT false;
ALTER TABLE users ADD COLUMN IF NOT EXISTS is_trainee boolean NOT NULL DEFAULT false;
ALTER TABLE users ADD COLUMN IF NOT EXISTS mentor_uid text;
```

---

## 四、關鍵程式碼片段(backend/main.py)

**S3 一例一休上限(凸性，全職)**
```python
if not is_ht:
    for _th in (2, 3, 4):   # 門檻遞增→凸性→逼每週OFF平均、收斂為2
        _ov = model.new_int_var(0, len(rest_eligible), f"wkov_{m}_{wi}_{_th}")
        model.add(_ov >= week_rest_sum - _th)
        penalties.append(_ov * WEEKLY_OFF_OVER_PENALTY)   # 預設 500
```

**新人:不計人數 + 跟隨導師**
```python
trainee_set = {i for i, nu in enumerate(nurses) if nu.get("is_trainee")}
mentor_of = {}
for i, nu in enumerate(nurses):   # ⚠️ 別用 n 當變數!會外洩覆蓋週期天數 n
    if nu.get("is_trainee") and nu.get("mentor_uid"):
        mj = nid.get(nu["mentor_uid"])
        if mj is not None and mj not in trainee_set and mj != i: mentor_of[i] = mj
# S1/S11/S12 排除:  ...for m in range(M) if (m,t) not in admin_cells and m not in trainee_set
for _ti, _mj in mentor_of.items():
    for t in range(n):
        _md = model.new_bool_var(f"mentor_diff_{_ti}_{t}")
        model.add(x[_ti][t] != x[_mj][t]).only_enforce_if(_md)
        model.add(x[_ti][t] == x[_mj][t]).only_enforce_if(_md.negated())
        penalties.append(_md * MENTOR_FOLLOW_PENALTY)   # 預設 3000(要夠高才壓過換班1500/比例900)
```

**env 旋鈕(設在 Railway)**:`FAIR_FIX_DEV`(0)、`DIAG_SOLVE_SECONDS`(45)、`WEEKLY_OFF_OVER_PENALTY`(500)、`MENTOR_FOLLOW_PENALTY`(3000)、`EXCESS_SWITCH_PENALTY`(1500)、`DIRECT_SWITCH_PENALTY`(500)。

**CP-SAT 懲罰值**:順班多餘換班1500/沒休就換500、比例偏差900、孤立日750、固定班偏離500/格、每週超休500、超休500、應休縮減200、跟隨導師3000。solver `max_time=90s, num_workers=4`。

---

## 五、正在解決 / 未解

1. **Railway Trial 快到期**:一次性 $5，到期後端會停 → 需升 Hobby($5/月)。前端/DB 免費不受影響。
2. **人力過剩導致 OFF > 應休**:可提供 300 人力天 vs 需求 280 → 多 20 天必然 OFF，規則消不掉(只能凸性懲罰平均攤)。要更多人剛好每週2就得調高每日需求。用戶已決定「就這樣」。
3. **fair 固定班=0 過度約束**:某些月份 fair 會 INFEASIBLE/90s 逾時 UNKNOWN。可 `FAIR_FIX_DEV=1` 臨時放寬。
4. 求解常 90s 逾時只拿 FEASIBLE(非最佳)，模型重(~9500 變數)。

---

## 六、下一步(候選)

1. **新人+導師用真實新人帳號在線上驗證**(邏輯已本機實測通過:跟導師28/28、臨床0/28不符、每週OFF[2,2,2,2])。
2. Railway 到期前升 Hobby。
3. `/data` 學習系統:HomePage 已指向 /data，但 App.tsx 尚無此路由與頁面(點會導回登入)。用戶自己在弄 game-prototype(見 game-prototype.md 記憶)。
4. 後端內部變數名(S1/S8/S11…)未跟 H/S 重編(刻意不動)。

---

## 七、慣例 / 踩過的坑

- 改完前後端各自部署;「改了沒生效」多半忘部署或瀏覽器快取(Ctrl+Shift+R)。
- 本機測後端唯讀:`generate_schedule` 只讀不寫(寫在 /schedule/commit)，可 `main.generate_schedule(profile=..., current_user={"sub":"x","role":"admin"})` 直接跑。
- **改 DB schema 一律用 Supabase SQL Editor**;加欄位順序:先 ALTER TABLE → 再部署後端(否則 SELECT 新欄位會 500)。
- 坑:①`for i, n in enumerate(...)` 的 `n` 外洩覆蓋週期天數→有新人就崩(改名 `nu`);②wrangler 要在 frontend 目錄;③npx 快取偶爾損壞(workerd 平台錯)→ `rm -rf ~/AppData/Local/npm-cache/_npx/<hash>` 重跑;④本機 curl 有 Windows SSL 撤銷檢查問題→加 `--ssl-no-revoke` 或用瀏覽器查 bundle 名確認部署。
